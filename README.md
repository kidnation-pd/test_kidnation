# Continuous Integration / Continuous Deployment

CI/CD (Continuous Integration / Continuous Deployment) — это практика автоматизации процессов разработки, тестирования и развертывания программного обеспечения.

## 🔄 CI (Continuous Integration) - Непрерывная интеграция

**Что это:**

* Разработчики часто объединяют свой код с основной веткой (например, main)
* Автоматическая проверка каждого изменения

**Процесс:**

```bash
# Вы делаете коммит в Git → запускается автоматический пайплайн:
1. Сборка приложения
2. Запуск тестов
3. Проверка качества кода
```
**Пример инструментов:** Jenkins, GitLab CI, GitHub Actions, CircleCI



## 🚀 CD (Continuous Deployment/Delivery) - Непрерывное развертывание/поставка

**Что это:**

* Автоматическое развертывание проверенного кода на серверы
* Постоянная доступность новой версии для пользователей

**Процесс:**

```bash
# После успешного CI:
4. Сборка Docker-образа
5. Развертывание на тестовый сервер
6. Автотесты на тестовом окружении
7. Развертывание на продакшен
```

## 🎯 Простой пример пайплайна:

```yaml
# .gitlab-ci.yml пример
stages:
  - test
  - build
  - deploy

test:
  stage: test
  script:
    - npm install
    - npm test

build:
  stage: build
  script:
    - docker build -t my-app .

deploy:
  stage: deploy
  script:
    - kubectl apply -f k8s/
```

## 💡 Преимущества CI/CD:

✅ **Раннее обнаружение ошибок - тесты запускаются автоматически**
✅ **Быстрое обновление - новые фичи быстрее доходят до пользователей**
✅ **Снижение рисков - автоматизация уменьшает человеческие ошибки**
✅ **Постоянная готовность к релизу - код всегда в рабочем состоянии**

## 🔧 Типичный workflow:
1. Программист пушит код в Git
2. CI-сервер автоматически:
    * Запускает сборку
    * Выполняет тесты
    * Проверяет качество кода
3. CD-система автоматически:
    * Разворачивает на тестовое окружение
    * Запускает интеграционные тесты
    * Разворачивает на продакшен (если все тесты пройдены)


## 🛠 Популярные инструменты:

* GitHub Actions (интегрирован с GitHub)
* GitLab CI (интегрирован с GitLab)
* Jenkins (самостоятельный сервер)
* Azure DevOps (Microsoft)
* CircleCI (облачный сервис)


**По сути, CI/CD — это автоматизация всего процесса от написания кода до его попадания к пользователям!** 🎯